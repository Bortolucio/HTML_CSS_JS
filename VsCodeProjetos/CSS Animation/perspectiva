<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!--<link rel = "stylesheet" href="style.css">-->
    <title>Transform: perspective</title>
    <style>
        body{
            font-family: sans-serif;
            margin-top: 150px;
            font-size: 30px;
        }
        .wrap {
            width: 200px;
            height: 200px;
            margin: 50px auto;
            background-color: #f5f5f5;
            border: 1px #aaa solid;
            border-radius: 5px;
            box-shadow: 0px 3px 6px rgba(0,0,0,.5);
        }
        .box {
            background-color: rgba(0,221,244, .6);
            border: 1ppx #3cf solid;
            border-radius: 5px;
            box-sizing: border-box;
            width: 200px;
            height: 200px;
            padding: 20px;
            text-align: center;
            transform: perspective(120px) rotateY(45deg);
        } /* altere o valor em perspective() para resultados diferentes*/
 
    </style>
</head>
<body>
    <div class="wrap">
        <div class="box"> Perspectiva</div>
    </div>

</body>
</html>
